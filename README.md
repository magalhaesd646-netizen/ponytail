This branch hosts only the static build of `whatsapp-team-draw` via GitHub Pages.

Don't edit files here by hand — regenerate from the source branch instead:

```bash
node whatsapp-team-draw/scripts/build-standalone.js
cp "whatsapp-team-draw/public/standalone.html" <path-to-this-worktree>/index.html
```
